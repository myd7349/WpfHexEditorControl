<Window x:Class="WpfHexaEditor.Views.Panels.JumpToOffsetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Jump to Offset"
        Width="400" Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Enter offset (hex or decimal):"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <TextBox x:Name="OffsetTextBox"
                 Grid.Row="1"
                 FontFamily="Consolas"
                 FontSize="13"
                 Padding="8,4"
                 Margin="0,0,0,8"
                 TextChanged="OffsetTextBox_TextChanged"/>

        <TextBlock Grid.Row="2"
                   Text="Examples: 0x100, 256, 0x1A4"
                   FontSize="10"
                   Foreground="#666666"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Jump"
                    Width="80"
                    Height="28"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="JumpButton_Click"/>
            <Button Content="Cancel"
                    Width="80"
                    Height="28"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
