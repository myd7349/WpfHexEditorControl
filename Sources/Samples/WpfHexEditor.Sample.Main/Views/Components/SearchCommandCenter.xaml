<!--
    Apache 2.0  2026
    HexEditor V2 - Search Bar (Office Style)
    Clean and professional search bar inspired by Microsoft Office
    Author : Derek Tremblay (derektremblay666@gmail.com)
    Contributors: Claude Sonnet 4.5
-->

<UserControl x:Class="WpfHexEditor.Sample.Main.Views.Components.SearchCommandCenter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfHexEditor.Sample.Main.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="500"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <!-- Main Container -->
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Bar Row -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box -->
            <Border Grid.Column="0"
                    Background="{StaticResource SurfaceElevatedBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="3"
                    Height="32"
                    Margin="0,0,8,0">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Icon -->
                    <TextBlock Grid.Column="0"
                              Text="🔍"
                              FontSize="14"
                              VerticalAlignment="Center"
                              Margin="8,0,6,0"
                              Opacity="0.6"/>

                    <!-- Search TextBox -->
                    <TextBox Grid.Column="1"
                            x:Name="SearchTextBox"
                            Text="{Binding SearchPattern, UpdateSourceTrigger=PropertyChanged}"
                            FontSize="13"
                            FontFamily="Segoe UI"
                            BorderThickness="0"
                            Background="Transparent"
                            Foreground="{StaticResource ForegroundBrush}"
                            CaretBrush="{StaticResource AccentBrush}"
                            VerticalAlignment="Center"
                            VerticalContentAlignment="Center">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding FindAllCommand}"/>
                            <KeyBinding Key="F3" Command="{Binding FindNextCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <!-- Mode Selector (Dropdown-style) -->
                    <ComboBox Grid.Column="2"
                             SelectedIndex="0"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{StaticResource ForegroundSecondaryBrush}"
                             FontSize="11"
                             VerticalAlignment="Center"
                             Margin="4,0">
                        <ComboBoxItem Content="Text" IsSelected="{Binding IsTextMode}"/>
                        <ComboBoxItem Content="Hex" IsSelected="{Binding IsHexMode}"/>
                        <ComboBoxItem Content="Regex" IsSelected="{Binding IsRegexMode}"/>
                    </ComboBox>

                    <!-- Clear Button -->
                    <Button Grid.Column="3"
                           Content="✕"
                           FontSize="12"
                           Width="24"
                           Height="24"
                           Margin="2,0,4,0"
                           Style="{StaticResource ModernButtonStyle}"
                           Command="{Binding ClearSearchCommand}"
                           ToolTip="Clear"
                           Visibility="{Binding HasSearchPattern, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="◀"
                       Width="28"
                       Height="32"
                       Style="{StaticResource ModernButtonStyle}"
                       Command="{Binding FindPreviousCommand}"
                       IsEnabled="{Binding HasResults}"
                       ToolTip="Previous (Shift+F3)"
                       Margin="0,0,4,0"/>

                <Button Content="▶"
                       Width="28"
                       Height="32"
                       Style="{StaticResource ModernButtonStyle}"
                       Command="{Binding FindNextCommand}"
                       IsEnabled="{Binding HasResults}"
                       ToolTip="Next (F3)"
                       Margin="0,0,4,0"/>

                <Button Content="Find All"
                       Height="32"
                       Padding="12,0"
                       Style="{StaticResource AccentButtonStyle}"
                       Command="{Binding FindAllCommand}"
                       IsEnabled="{Binding CanSearch}"
                       ToolTip="Find All Occurrences"/>
            </StackPanel>
        </Grid>

        <!-- Results Info (Compact) -->
        <StackPanel Grid.Row="1"
                   Orientation="Horizontal"
                   Margin="8,6,0,0"
                   Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibilityConverter}}">

            <TextBlock FontSize="11"
                      Foreground="{StaticResource ForegroundSecondaryBrush}"
                      VerticalAlignment="Center">
                <Run Text="{Binding ResultCount, Mode=OneWay}"/>
                <Run Text="matches"/>
            </TextBlock>

            <TextBlock Text="•"
                      FontSize="11"
                      Foreground="{StaticResource ForegroundTertiaryBrush}"
                      Margin="8,0"/>

            <TextBlock Text="{Binding SearchDurationMs, StringFormat={}{0:F1}ms}"
                      FontSize="11"
                      Foreground="{StaticResource SuccessBrush}"
                      VerticalAlignment="Center"/>

            <TextBlock Text="•"
                      FontSize="11"
                      Foreground="{StaticResource ForegroundTertiaryBrush}"
                      Margin="8,0"/>

            <TextBlock Text="{Binding SearchSpeedMBps, StringFormat={}{0:F0} MB/s}"
                      FontSize="11"
                      Foreground="{StaticResource InfoBrush}"
                      VerticalAlignment="Center"/>

            <Border Background="{StaticResource SuccessBrush}"
                   CornerRadius="2"
                   Padding="4,2"
                   Margin="12,0,0,0"
                   VerticalAlignment="Center">
                <TextBlock Text="V2"
                          FontSize="9"
                          FontWeight="Bold"
                          Foreground="#FFFFFF"/>
            </Border>
        </StackPanel>

        <!-- Progress Bar -->
        <ProgressBar Grid.Row="0"
                    Height="1"
                    VerticalAlignment="Bottom"
                    IsIndeterminate="True"
                    Foreground="{StaticResource AccentBrush}"
                    Background="Transparent"
                    BorderThickness="0"
                    Visibility="{Binding IsSearching, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</UserControl>
