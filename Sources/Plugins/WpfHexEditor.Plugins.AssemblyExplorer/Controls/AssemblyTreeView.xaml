<!--
    Project: WpfHexEditor.Plugins.AssemblyExplorer
    File: Controls/AssemblyTreeView.xaml
    Author: Derek Tremblay
    Created: 2026-03-08
    Description:
        Custom TreeView for the Assembly Explorer panel.
        Uses VS-style icons (Segoe MDL2 Assets) beside each node label.
        VirtualizingPanel enabled + Recycling mode for large assemblies.
        Theme: DynamicResource PFP_* brush tokens.
-->
<UserControl x:Class="WpfHexEditor.Plugins.AssemblyExplorer.Controls.AssemblyTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WpfHexEditor.Plugins.AssemblyExplorer.ViewModels"
             xmlns:conv="clr-namespace:WpfHexEditor.Plugins.AssemblyExplorer.Converters">

    <UserControl.Resources>
        <conv:NodeTypeToIconConverter x:Key="NodeIconConverter"/>
        <conv:MemberAccessToVisibilityConverter x:Key="AccessConverter"/>

        <!-- Item container style: hover + selected brushes via PFP tokens -->
        <Style x:Key="AsmTreeItemStyle" TargetType="TreeViewItem">
            <Setter Property="IsExpanded"  Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="IsSelected"  Value="{Binding IsSelected, Mode=TwoWay}"/>
            <Setter Property="ToolTip"     Value="{Binding ToolTipText}"/>
            <Setter Property="Padding"     Value="2,1"/>
            <Setter Property="Foreground"  Value="{DynamicResource PFP_TextBrush}"/>
            <Setter Property="Background"  Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PFP_ItemHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PFP_ItemSelectedBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PFP_ItemSelectedForegroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Default node DataTemplate -->
        <HierarchicalDataTemplate DataType="{x:Type vm:AssemblyNodeViewModel}"
                                  ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal" Margin="0,1">
                <TextBlock Text="{Binding Converter={StaticResource NodeIconConverter}}"
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="0,0,4,0"
                           Foreground="{DynamicResource PFP_SubTextBrush}"/>
                <TextBlock Text="{Binding DisplayName}"
                           VerticalAlignment="Center"
                           FontSize="12"/>
            </StackPanel>
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <TreeView x:Name="InnerTreeView"
              Background="{DynamicResource PFP_PanelBackgroundBrush}"
              Foreground="{DynamicResource PFP_TextBrush}"
              BorderThickness="0"
              ItemContainerStyle="{StaticResource AsmTreeItemStyle}"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              ScrollViewer.HorizontalScrollBarVisibility="Auto"
              SelectedItemChanged="OnSelectedItemChanged">
        <TreeView.ContextMenu>
            <ContextMenu x:Name="NodeContextMenu"
                         Background="{DynamicResource DockMenuBackgroundBrush}"
                         BorderBrush="{DynamicResource DockBorderBrush}"
                         Foreground="{DynamicResource DockMenuForegroundBrush}"
                         Opened="OnContextMenuOpened">
                <MenuItem Header="Open in HexEditor"  x:Name="MenuOpenInHex"   Click="OnOpenInHexEditor"/>
                <MenuItem Header="Copy Name"           x:Name="MenuCopyName"    Click="OnCopyName"/>
                <MenuItem Header="Copy Full Name"      x:Name="MenuCopyFull"    Click="OnCopyFullName"/>
                <MenuItem Header="Copy IL Offset"      x:Name="MenuCopyOffset"  Click="OnCopyOffset"/>
                <Separator/>
                <MenuItem Header="Go to Definition"    IsEnabled="False" ToolTip="Coming in a future release"/>
                <MenuItem Header="Decompile to C#"     x:Name="MenuDecompile"   Click="OnDecompile"/>
                <MenuItem Header="Show in Metadata Tables" IsEnabled="False" ToolTip="Coming in a future release"/>
                <Separator/>
                <MenuItem Header="Export IL"           IsEnabled="False" ToolTip="Coming in a future release"/>
                <MenuItem Header="Export C#"           IsEnabled="False" ToolTip="Coming in a future release"/>
            </ContextMenu>
        </TreeView.ContextMenu>
    </TreeView>
</UserControl>
