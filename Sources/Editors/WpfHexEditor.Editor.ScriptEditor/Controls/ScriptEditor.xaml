<!--
    GNU Affero General Public License v3.0  2026
    Author : Derek Tremblay (derektremblay666@gmail.com)
    Contributors: Claude Sonnet 4.6
-->

<UserControl x:Class="WpfHexEditor.Editor.ScriptEditor.Controls.ScriptEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:scriptSvc="clr-namespace:WpfHexEditor.Editor.ScriptEditor.Services"
             xmlns:ce="clr-namespace:WpfHexEditor.Editor.CodeEditor.Controls;assembly=WpfHexEditor.Editor.CodeEditor">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <scriptSvc:ScriptEditorLocalizedDictionary/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Toolbar -->
            <RowDefinition Height="30" />
            <!-- Code area -->
            <RowDefinition Height="*" MinHeight="60" />
            <!-- Splitter -->
            <RowDefinition Height="4" />
            <!-- Output area -->
            <RowDefinition Height="120" MinHeight="40" />
            <!-- Status bar -->
            <RowDefinition Height="22" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0"
                Background="{DynamicResource TE_Background}"
                BorderBrush="{DynamicResource DockSplitterBrush}"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="6,0">
                <TextBlock x:Name="ScriptLabel"
                           VerticalAlignment="Center"
                           FontSize="11"
                           Foreground="{DynamicResource TE_LineNumberForeground}"
                           Text="{StaticResource SCR_F5Help}" />
            </StackPanel>
        </Border>

        <!-- Code editor (CodeEditorSplitHost — syntax + SmartComplete) -->
        <ce:CodeEditorSplitHost Grid.Row="1"
                                x:Name="CodeBox"
                                ShowMinimap="False"
                                ShowLineNumbers="True" />

        <!-- Splitter -->
        <GridSplitter Grid.Row="2"
                      HorizontalAlignment="Stretch"
                      Background="{DynamicResource DockSplitterBrush}"
                      ResizeDirection="Rows" />

        <!-- Output area -->
        <Border Grid.Row="3"
                BorderBrush="{DynamicResource DockSplitterBrush}"
                BorderThickness="0,1,0,0">
            <TextBox x:Name="OutputBox"
                     FontFamily="Consolas, Courier New, monospace"
                     FontSize="11"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     HorizontalScrollBarVisibility="Auto"
                     VerticalScrollBarVisibility="Auto"
                     Foreground="{DynamicResource TE_LineNumberForeground}"
                     Background="{DynamicResource TE_Background}"
                     BorderThickness="0"
                     Padding="6" />
        </Border>

        <!-- Status bar -->
        <Border Grid.Row="4"
                Background="{DynamicResource TE_Background}"
                BorderBrush="{DynamicResource DockSplitterBrush}"
                BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusBar"
                       VerticalAlignment="Center"
                       Margin="8,0"
                       FontSize="11"
                       Foreground="{DynamicResource TE_LineNumberForeground}"
                       Text="{StaticResource SCR_StatusReady}" />
        </Border>
    </Grid>
</UserControl>
