<!--
    GNU Affero General Public License v3.0  2026
    Author : Derek Tremblay (derektremblay666@gmail.com)
    Contributors: Claude Sonnet 4.6
-->

<UserControl x:Class="WpfHexEditor.Editor.CodeEditor.Controls.FormatScriptEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfHexEditor.Editor.CodeEditor.Controls"
             xmlns:codeEditor="clr-namespace:WpfHexEditor.Editor.CodeEditor.Controls"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             xmlns:codeSvc="clr-namespace:WpfHexEditor.Editor.CodeEditor.Services">


    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <codeSvc:CodeEditorLocalizedDictionary/>
            </ResourceDictionary.MergedDictionaries>
        <!-- Button Style -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Panel_ToolbarButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Panel_ToolbarButtonActiveBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <DockPanel Background="{DynamicResource TE_Background}">

        <!-- Toolbar -->
        <Border DockPanel.Dock="Top"
                BorderBrush="{DynamicResource Panel_ToolbarBorderBrush}"
                BorderThickness="0,0,0,1"
                Background="{DynamicResource Panel_ToolbarBrush}"
                Padding="4"
                TextElement.Foreground="{DynamicResource Panel_ToolbarForegroundBrush}">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="NewFileButton" Style="{StaticResource ToolbarButtonStyle}" ToolTip="{StaticResource FormatScriptEditor_NewToolTip}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xEB9F;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{StaticResource FormatScriptEditor_NewText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button x:Name="OpenFileButton" Style="{StaticResource ToolbarButtonStyle}" ToolTip="{StaticResource FormatScriptEditor_OpenToolTip}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8E5;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{StaticResource FormatScriptEditor_OpenText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button x:Name="SaveFileButton" Style="{StaticResource ToolbarButtonStyle}" ToolTip="{StaticResource FormatScriptEditor_SaveToolTip}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE74E;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{StaticResource FormatScriptEditor_SaveText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Separator Width="1" Margin="8,4" Background="{DynamicResource Panel_ToolbarBorderBrush}"/>

                <Button x:Name="UndoButton" Style="{StaticResource ToolbarButtonStyle}" ToolTip="{StaticResource FormatScriptEditor_UndoToolTip}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7A7;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{StaticResource FormatScriptEditor_UndoText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button x:Name="RedoButton" Style="{StaticResource ToolbarButtonStyle}" ToolTip="{StaticResource FormatScriptEditor_RedoToolTip}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7A6;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{StaticResource FormatScriptEditor_RedoText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Separator Width="1" Margin="8,4" Background="{DynamicResource Panel_ToolbarBorderBrush}"/>

                <Button x:Name="FormatButton" Style="{StaticResource ToolbarButtonStyle}" ToolTip="{StaticResource FormatScriptEditor_FormatToolTip}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{StaticResource FormatScriptEditor_FormatText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button x:Name="ValidateButton" Style="{StaticResource ToolbarButtonStyle}" ToolTip="{StaticResource FormatScriptEditor_ValidateToolTip}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8FB;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{StaticResource FormatScriptEditor_ValidateText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Status Bar -->
        <Border DockPanel.Dock="Bottom"
                BorderBrush="{DynamicResource Panel_ToolbarBorderBrush}"
                BorderThickness="0,1,0,0"
                Background="{DynamicResource Panel_ToolbarBrush}"
                Padding="8,4"
                TextElement.Foreground="{DynamicResource Panel_ToolbarForegroundBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Line/Column indicator -->
                <TextBlock x:Name="LineColumnText" Grid.Column="0" Text="{StaticResource FormatScriptEditor_LineColStatus}" Margin="0,0,16,0"/>

                <!-- Errors/Warnings -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,16,0">
                    <TextBlock Text="&#xEA39;" FontFamily="Segoe MDL2 Assets" FontSize="12" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ErrorCountText" Text="{StaticResource FormatScriptEditor_ErrorCountStatus}"/>
                    <TextBlock Text=" " Margin="4,0"/>
                    <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="12" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="WarningCountText" Text="{StaticResource FormatScriptEditor_WarningCountStatus}"/>
                </StackPanel>

                <!-- Current file name -->
                <TextBlock x:Name="FileNameText" Grid.Column="3" Text="{StaticResource FormatScriptEditor_NoFileLoaded}" HorizontalAlignment="Left"/>

                <!-- Encoding / File Type -->
                <TextBlock x:Name="FileTypeText" Grid.Column="4" Text="{StaticResource FormatScriptEditor_FileTypeStatus}" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="150"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <!-- File Tree (Left Panel) -->
            <Border Grid.Column="0"
                    BorderBrush="{DynamicResource Panel_ToolbarBorderBrush}"
                    BorderThickness="0,0,1,0"
                    Background="{DynamicResource TE_Background}">
                <DockPanel>
                    <Border DockPanel.Dock="Top"
                            Background="{DynamicResource SE_GroupHeaderBrush}"
                            BorderBrush="{DynamicResource Panel_ToolbarBorderBrush}"
                            BorderThickness="0,0,0,1">
                        <TextBlock Text="{StaticResource FormatScriptEditor_EmbeddedFormatsHeader}"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource Panel_ToolbarForegroundBrush}"
                                   Padding="8"/>
                    </Border>

                    <TreeView x:Name="FormatTreeView"
                              BorderThickness="0"
                              Background="{DynamicResource TE_Background}"
                              Foreground="{DynamicResource TE_Foreground}">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Icon}" Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding Name}"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </DockPanel>
            </Border>

            <!-- GridSplitter -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                          Background="{DynamicResource DockSplitterBrush}"/>

            <!-- Editor Area (Right Panel) -->
            <TabControl x:Name="MainTabControl" Grid.Column="2" BorderThickness="0"
                       SelectionChanged="MainTabControl_SelectionChanged">

                <!-- Editor Tab -->
                <TabItem x:Name="EditorTab" Header="{StaticResource FormatScriptEditor_EditorTabHeader}">
                    <Grid>
                        <codeEditor:CodeEditor x:Name="CodeEditorControl"
                                              ShowLineNumbers="True"
                                              EnableSmartComplete="True"
                                              EnableValidation="True"
                                              EditorBackground="{DynamicResource TE_Background}"
                                              EditorForeground="{DynamicResource TE_Foreground}"
                                              LineNumberBackground="{DynamicResource TE_LineNumberBackground}"
                                              LineNumberForeground="{DynamicResource TE_LineNumberForeground}"
                                              CurrentLineBackground="{DynamicResource TE_CurrentLineBrush}"
                                              SelectionBackground="{DynamicResource TE_SelectionBackground}"
                                              SyntaxKeyColor="{DynamicResource TE_Keyword}"
                                              SyntaxKeywordColor="{DynamicResource TE_Keyword}"
                                              SyntaxBooleanColor="{DynamicResource TE_Keyword}"
                                              SyntaxStringValueColor="{DynamicResource TE_String}"
                                              SyntaxNumberColor="{DynamicResource TE_Number}"
                                              SyntaxCommentColor="{DynamicResource TE_Comment}"
                                              SyntaxValueTypeColor="{DynamicResource TE_Type}"
                                              SyntaxCalcExpressionColor="{DynamicResource TE_Directive}"
                                              SyntaxVariableReferenceColor="{DynamicResource TE_Register}"
                                              SyntaxEscapeSequenceColor="{DynamicResource TE_Label}"
                                              SyntaxUrlColor="{DynamicResource TE_Register}"
                                              SyntaxBraceColor="{DynamicResource TE_Foreground}"
                                              SyntaxBracketColor="{DynamicResource TE_Foreground}"
                                              SyntaxCommaColor="{DynamicResource TE_Foreground}"
                                              SyntaxColonColor="{DynamicResource TE_Foreground}"
                                              SyntaxNullColor="{DynamicResource TE_Operator}"
                                              SyntaxDeprecatedColor="{DynamicResource TE_Operator}"/>
                    </Grid>
                </TabItem>

                <!-- Preview Tab -->
                <TabItem Header="{StaticResource FormatScriptEditor_PreviewTabHeader}">
                    <ScrollViewer Background="{DynamicResource TE_Background}">
                        <TextBlock x:Name="PreviewTextBlock"
                                  Padding="10"
                                  FontFamily="Consolas"
                                  FontSize="12"
                                  TextWrapping="Wrap"
                                  Foreground="{DynamicResource TE_Foreground}"
                                  Text="{StaticResource FormatScriptEditor_PreviewPlaceholder}"/>
                    </ScrollViewer>
                </TabItem>

                <!-- Validation Tab -->
                <TabItem Header="{StaticResource FormatScriptEditor_ValidationTabHeader}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Padding="8"
                                  Background="{DynamicResource SE_GroupHeaderBrush}"
                                  Foreground="{DynamicResource Panel_ToolbarForegroundBrush}"
                                  Text="{StaticResource FormatScriptEditor_ValidationResultsHeader}" FontWeight="Bold"/>

                        <ListBox x:Name="ValidationListBox"
                                 BorderThickness="0"
                                 Background="{DynamicResource TE_Background}"
                                 Foreground="{DynamicResource TE_Foreground}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="4">
                                        <TextBlock Text="{Binding Icon}" FontSize="16" Margin="0,0,8,0"/>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Message}" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Location}"
                                                       Foreground="{DynamicResource PFP_SubTextBrush}"
                                                       FontSize="11"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>
                </TabItem>

                <!-- Test Tab -->
                <TabItem Header="{StaticResource FormatScriptEditor_TestTabHeader}">
                    <DockPanel>
                        <Border DockPanel.Dock="Top" Padding="8"
                                Background="{DynamicResource SE_GroupHeaderBrush}">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="{StaticResource FormatScriptEditor_SelectTestFileButton}" Padding="8,4" Margin="0,0,8,0"/>
                                <Button Content="{StaticResource FormatScriptEditor_RunTestButton}" Padding="8,4"/>
                            </StackPanel>
                        </Border>

                        <TextBlock Padding="10" TextWrapping="Wrap"
                                  Foreground="{DynamicResource TE_Foreground}"
                                  Text="{StaticResource FormatScriptEditor_TestInstructions}"/>
                    </DockPanel>
                </TabItem>

            </TabControl>
        </Grid>
    </DockPanel>
</UserControl>
