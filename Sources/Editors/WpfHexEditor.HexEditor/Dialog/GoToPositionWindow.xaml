<!--
    GNU Affero General Public License v3.0  2026
    Author : Derek Tremblay (derektremblay666@gmail.com)
    Contributors: Claude Sonnet 4.6
-->

<ec:ThemedDialog
    x:Class="WpfHexEditor.HexEditor.Dialog.GoToPositionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ec="clr-namespace:WpfHexEditor.Editor.Core.Views;assembly=WpfHexEditor.Editor.Core"
    xmlns:hexSvc="clr-namespace:WpfHexEditor.HexEditor.Services"
    Title="{DynamicResource GoToPositionTitle}"
    Width="380"
    Height="210"
    x:ClassModifier="public"
    Background="{DynamicResource DockWindowBackgroundBrush}"
    mc:Ignorable="d">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <hexSvc:HexEditorLocalizedDictionary/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Position Input GroupBox -->
        <GroupBox Header="{StaticResource GoToPositionHeader}" Grid.Row="0" Margin="0,0,0,8">
            <Grid Margin="8,6,8,6">
                <TextBox
                    x:Name="PositionTextBox"
                    Height="26"
                    TabIndex="0"
                    VerticalContentAlignment="Center"
                    Padding="4,0"
                    TextChanged="PositionTextBox_TextChanged"/>
                <TextBlock
                    Text="{StaticResource GoToPositionPlaceholder}"
                    IsHitTestVisible="False"
                    VerticalAlignment="Center"
                    Margin="6,0,0,0"
                    Foreground="#999999">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=PositionTextBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </GroupBox>

        <!-- Mode RadioButtons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <RadioButton
                x:Name="HexRadio"
                Content="{StaticResource HexadecimalString}"
                IsChecked="True"
                Margin="0,0,16,0"
                TabIndex="1"
                Checked="ModeRadio_Changed"/>
            <RadioButton
                x:Name="DecimalRadio"
                Content="{StaticResource DecimalString}"
                TabIndex="2"
                Checked="ModeRadio_Changed"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button
                x:Name="OkButton"
                Content="{StaticResource OkString}"
                Width="90"
                Height="28"
                Margin="0,0,8,0"
                IsDefault="True"
                IsEnabled="False"
                TabIndex="3"
                Click="OkButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#0078D4"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderBrush" Value="#0078D4"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button
                Content="{StaticResource CancelString}"
                Width="90"
                Height="28"
                IsCancel="True"
                TabIndex="4">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#F3F3F3"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="BorderBrush" Value="#CCCCCC"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E1E1E1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</ec:ThemedDialog>
