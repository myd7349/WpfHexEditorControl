<Window x:Class="WpfHexEditor.Editor.DocumentEditor.Dialogs.HyperlinkInsertDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:docSvc="clr-namespace:WpfHexEditor.Editor.DocumentEditor.Services"
        Title="{DynamicResource HyperlinkDlg_Title}"
        Width="400" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <docSvc:DocumentEditorLocalizedDictionary/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource HyperlinkDlg_LabelText}"
                   VerticalAlignment="Center"/>
        <TextBox   Grid.Row="0" Grid.Column="2" x:Name="PART_DisplayText"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource HyperlinkDlg_LabelUrl}"
                   VerticalAlignment="Center"/>
        <TextBox   Grid.Row="2" Grid.Column="2" x:Name="PART_Url"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="{DynamicResource HyperlinkDlg_Cancel}"
                    Width="80" Margin="0,0,8,0"
                    IsCancel="True" Click="OnCancelClicked"/>
            <Button x:Name="PART_OkBtn"
                    Content="{DynamicResource HyperlinkDlg_OK}"
                    Width="80" IsDefault="True" Click="OnOkClicked"/>
        </StackPanel>
    </Grid>
</Window>
