<Window x:Class="WpfHexEditor.Editor.ClassDiagram.Dialogs.RoundTripPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cdSvc="clr-namespace:WpfHexEditor.Editor.ClassDiagram.Services"
        Title="{DynamicResource ClassDiagEd_RoundTripPreview_Title}"
        Width="900" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Panel_Background}"
        Foreground="{DynamicResource Panel_Foreground}"
        ShowInTaskbar="False">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <cdSvc:ClassDiagramEditorLocalizedDictionary/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header: file path + edit kind -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock x:Name="HeaderFileText"
                       FontWeight="Bold"
                       Foreground="{DynamicResource Panel_Foreground}"/>
            <TextBlock x:Name="HeaderEditText"
                       Margin="0,2,0,0"
                       Opacity="0.8"
                       Foreground="{DynamicResource Panel_Foreground}"/>
        </StackPanel>

        <!-- Side-by-side before / after -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top"
                           Text="{StaticResource ClassDiagEd_RoundTripPreview_Before}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>
                <TextBox x:Name="BeforeBox"
                         IsReadOnly="True"
                         FontFamily="Consolas"
                         FontSize="12"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Background="{DynamicResource CD_ClassBoxBackground}"
                         Foreground="{DynamicResource Panel_Foreground}"
                         BorderBrush="{DynamicResource Panel_BorderBrush}"/>
            </DockPanel>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="{DynamicResource Panel_BorderBrush}"/>

            <DockPanel Grid.Column="2">
                <TextBlock DockPanel.Dock="Top"
                           Text="{StaticResource ClassDiagEd_RoundTripPreview_After}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>
                <TextBox x:Name="AfterBox"
                         IsReadOnly="True"
                         FontFamily="Consolas"
                         FontSize="12"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Background="{DynamicResource CD_ClassBoxBackground}"
                         Foreground="{DynamicResource Panel_Foreground}"
                         BorderBrush="{DynamicResource Panel_BorderBrush}"/>
            </DockPanel>
        </Grid>

        <!-- "Don't ask again" -->
        <CheckBox Grid.Row="2"
                  x:Name="DontAskAgainBox"
                  Margin="0,10,0,0"
                  Content="{StaticResource ClassDiagEd_RoundTripPreview_DontAskAgain}"
                  Foreground="{DynamicResource Panel_Foreground}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button x:Name="CancelButton"
                    Content="{StaticResource ClassDiagEd_RoundTripPreview_Cancel}"
                    MinWidth="90"
                    Padding="10,4"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Click="OnCancelClick"/>
            <Button x:Name="ApplyButton"
                    Content="{StaticResource ClassDiagEd_RoundTripPreview_Apply}"
                    MinWidth="90"
                    Padding="10,4"
                    IsDefault="True"
                    Click="OnApplyClick"/>
        </StackPanel>
    </Grid>
</Window>
