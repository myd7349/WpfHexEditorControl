<!--
    GNU Affero General Public License v3.0  2026
    Author : Derek Tremblay (derektremblay666@gmail.com)
    Contributors: Claude Sonnet 4.6

    Themed single-line (or multiline) input dialog.
    All brushes are DynamicResource — reacts to Dark / Light / High Contrast switches.
-->

<ec:ThemedDialog x:Class="WpfHexEditor.Editor.Core.Dialogs.IdeInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ec="clr-namespace:WpfHexEditor.Editor.Core.Views"
        SizeToContent="WidthAndHeight"
        MinWidth="420" MaxWidth="680"
        Background="{DynamicResource DockWindowBackgroundBrush}">

    <StackPanel Margin="20,16,20,16">

        <!-- Optional icon + prompt label row -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="IconGlyph"
                       Grid.Column="0"
                       FontFamily="Segoe MDL2 Assets"
                       FontSize="22"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"
                       Visibility="Collapsed" />

            <TextBlock x:Name="PromptText"
                       Grid.Column="1"
                       FontSize="13"
                       Foreground="{DynamicResource DockMenuForegroundBrush}"
                       TextWrapping="Wrap" />
        </Grid>

        <!-- Input area (single-line or multiline) -->
        <Border x:Name="InputBorder"
                BorderThickness="1"
                Margin="0,0,0,6">
            <Border.BorderBrush>
                <SolidColorBrush x:Name="InputBorderBrush" Color="#555555" />
            </Border.BorderBrush>
            <Border.Background>
                <DynamicResource ResourceKey="DockBackgroundBrush" />
            </Border.Background>

            <!-- Watermark + TextBox overlay -->
            <Grid>
                <TextBox x:Name="InputBox"
                         Padding="6,5"
                         FontSize="13"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{DynamicResource DockMenuForegroundBrush}"
                         CaretBrush="{DynamicResource DockMenuForegroundBrush}"
                         SelectionBrush="#6B3FA0"
                         AcceptsReturn="False"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Hidden"
                         HorizontalScrollBarVisibility="Hidden"
                         FocusVisualStyle="{x:Null}" />

                <TextBlock x:Name="WatermarkText"
                           Padding="8,6"
                           FontSize="13"
                           IsHitTestVisible="False"
                           VerticalAlignment="Top"
                           Opacity="0.45"
                           Foreground="{DynamicResource DockMenuForegroundBrush}"
                           Visibility="Collapsed" />
            </Grid>
        </Border>

        <!-- Optional secondary hint line (e.g. "Powered by Claude API") -->
        <TextBlock x:Name="HintText"
                   FontSize="11"
                   Margin="0,0,0,14"
                   Opacity="0.55"
                   Foreground="{DynamicResource DockMenuForegroundBrush}"
                   Visibility="Collapsed" />

        <!-- Button row -->
        <StackPanel x:Name="ButtonPanel"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Padding"          Value="14,5" />
                    <Setter Property="MinWidth"         Value="88" />
                    <Setter Property="Margin"           Value="8,0,0,0" />
                    <Setter Property="Cursor"           Value="Hand" />
                    <Setter Property="Foreground"       Value="{DynamicResource DockMenuForegroundBrush}" />
                    <Setter Property="Background"       Value="{DynamicResource DockBackgroundBrush}" />
                    <Setter Property="BorderBrush"      Value="{DynamicResource DockBorderBrush}" />
                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Root"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1">
                                    <Border x:Name="Overlay" Background="Transparent">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}"
                                                          RecognizesAccessKey="True" />
                                    </Border>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Overlay" Property="Background"  Value="#20FFFFFF" />
                                        <Setter TargetName="Root"    Property="BorderBrush" Value="#A070D0" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="Overlay" Property="Background" Value="#40FFFFFF" />
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.45" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </StackPanel.Resources>
        </StackPanel>

    </StackPanel>
</ec:ThemedDialog>
